# Eijkim Maulit — Developer Portfolio

Personal portfolio website built with React, Vite, TypeScript, and TailwindCSS.

## Run locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the dev server:
   `npm run dev`
3. Open `http://localhost:3000`